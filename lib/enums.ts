export enum CommuncationEnum {
    LIKE = "type",
    WE_MET = "we_met",
    MATCH = "match",
    BLOCK = "block",
    CHAT = "chats"
}

export enum WE_MET_STATUS_ENUM {
    notYet = "Not yet",
    MET_UP = "Yes"
}

export enum BlockTypeEum {
    REMOVE = "remove"
}